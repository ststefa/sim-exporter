apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "sim-exporter.fullname" . }}-config
  labels:
  {{- include "sim-exporter.selectorLabels" . | nindent 8 }}
data:
  libvirt-config.yaml: |
    {{- .Files.Get "examples/libvirt_converted.yaml" | nindent 4 -}}
  node-exporter.yaml: |
    {{- .Files.Get "examples/node_exporter.yaml" | nindent 4 -}}